baseURL = 'https://genesisbio.com/'
languageCode = 'en-us'
title = '元郁有限公司'
theme = 'genesis'

[params]
  description = '元郁有限公司 — Antibodies, Peptides, Proteins, ELISA kits, and custom services.'
  copyright = '元郁有限公司'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true   # 保留 legacy <TABLE> 內容
  [markup.tableOfContents]
    endLevel = 2
  [markup.highlight]
    noClasses = false
